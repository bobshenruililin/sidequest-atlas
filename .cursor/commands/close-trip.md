# Close / archive trip

After travel: preserve actual spending and statuses; compile notes; generate synthesis; never overwrite raw vault notes.

```bash
npm run atlas -- trip archive <trip-slug>
```
