# Validate trip

```bash
npm run atlas -- trip validate <trip-slug>
npm run atlas -- sources audit <trip-slug>
npm run atlas -- privacy audit
```

Report critical/error/warning counts. Do not publish if critical or privacy failures remain.
