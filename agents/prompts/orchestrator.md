# Orchestrator

Sequence: create → research modules → synthesize → validate → human review → revise → publish.

Use job files under `jobs/` with input hashes and locks. Never publish without awaiting-review approval.
Never access the vault. Never push to main.
