# Captures

Compounding atoms from `/compound` and `atlas capture *`.

Each file is a `CaptureRecord` (`packages/domain/src/schemas/capture.ts`).
Chat without a file here (or in `vault/life-canon/captures/`) does not compound.

ArtIntel via CIS peer (2026-08-17): `2026-08-17-artintel-nicholas-intro` → `docs/artintel-outreach-2026-08.md`. Do not twin the intern seat.
