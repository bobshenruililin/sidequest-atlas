# Claims folder

On this computer, store live YAML here: `claims/`. Shape: `../schemas/*example.yaml`. Copy the shape, not 2026 dates, when writing 2027 claims.

Sourced 22 Aug 2026 files (`claim-*` + `src-*`) are the operator re-check of Scout’s first cycle. Scout must still attach *its* 27-row CSV; do not merge a homemade skeleton into these files.
