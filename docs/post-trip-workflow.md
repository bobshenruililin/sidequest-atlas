# Post-trip workflow

```bash
npm run atlas -- trip archive <slug>
```

Preserve:

- actual spending
- completed / skipped statuses
- structured observations
- planned vs actual comparison
- final synthesis

Never overwrite raw vault notes. Published essays link back via `vault://` references.
